{
  "name": "themer",
  "build": {
    "dockerfile": "./Dockerfile"
  },
  "remoteUser": "node",
  "extensions": ["esbenp.prettier-vscode"],
  "postCreateCommand": "cd cli && npm install && cd ../web && npm install --legacy-peer-deps"
}
