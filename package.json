{
  "name": "themer-iterm",
  "version": "1.0.0",
  "description": "iTerm2 theme generator for themer.",
  "main": "lib/index.js",
  "author": "mjswensen",
  "license": "MIT",
  "jest": {
    "rootDir": "lib"
  },
  "scripts": {
    "build": "babel --out-dir lib src",
    "start": "watch 'yarn run build' src",
    "prepublish": "yarn run build",
    "test": "yarn run build; jest"
  },
  "dependencies": {
    "color": "^1.0.3"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0",
    "jest": "^18.0.0",
    "watch": "^1.0.1"
  },
  "repository": {
    "url": "git@github.com:mjswensen/themer-iterm.git",
    "type": "git"
  },
  "peerDependencies": {
    "themer": "^1.0.2"
  }
}
