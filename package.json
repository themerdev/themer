{
  "name": "themer-sublime-text",
  "version": "1.0.1",
  "description": "Sublime Text theme generator for themer.",
  "main": "lib/index.js",
  "author": "mjswensen",
  "license": "MIT",
  "jest": {
    "rootDir": "lib"
  },
  "scripts": {
    "build": "eslint src && babel --out-dir lib src",
    "prepublish": "yarn run build",
    "start": "watch 'yarn run test' src",
    "test": "yarn run build && jest"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0",
    "eslint": "^3.12.2",
    "jest": "^18.0.0",
    "themer-colors-default": "^1.0.1",
    "watch": "^1.0.1"
  },
  "dependencies": {
    "plist": "^2.0.1"
  },
  "repository": {
    "url": "git@github.com:mjswensen/themer-sublime-text.git",
    "type": "git"
  }
}
