{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write .",
    "test:format": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "2.3.1"
  }
}
